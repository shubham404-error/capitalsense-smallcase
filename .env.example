# Copy to .env for local dev. In production set these in
# Netlify → Site settings → Environment variables.

# Gemini API key for the research assistant (/api/chat).
GEMINI_API_KEY=

# Optional. Comma-separated origins permitted to call the functions.
# Leave unset to allow same-origin only.
ALLOWED_ORIGIN=
